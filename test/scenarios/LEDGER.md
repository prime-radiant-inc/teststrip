# Scenario Ledger — canonical status tracker

Single writer: the story-loop main thread. One row per capability card.
Status flow: Spec'd → Tested-Pass | Tested-Fail → Fixed → Verified.

| ID | Card | Status | Test method | Defect type | Actual result | Notes / open questions |
|---|---|---|---|---|---|---|
| cull-001-workspace-key-gating | cull-001-workspace-key-gating.md — Cull keyboard shortcuts stay scoped to the Cull lens, and never fire in a text field | Reconciled — NOT re-run | VM e2e (ax+sql) | — | reconciled 2026-08-09 (Task 13, unified-shell push): retitled "...scoped to the Cull lens"; `CullingKeyCaptureGate.isActive(workspace:selectedView:)` renamed to `isActive(lens:selectedView:)` (predicate byte-identical); drifted text-editor-guard citations corrected | supersedes prior status: the iter2 PASS covered the deleted `workspace:`-labeled gate signature, which no longer compiles — needs a fresh VM run |
| cull-002-loupe-navigation | cull-002-loupe-navigation.md — Left/Right stack nav, Up/Down within-stack nav, Space, and end-of-scope pagination in the Cull loupe | Reconciled — NOT re-run | VM e2e (ax+sql) | — | reconciled 2026-07-13 (cull-stack-rail branch) to the remapped arrows (↑/↓ now within-stack, ←/→ now across-stack landing on the recommended frame; ⌥←/⌥→ deleted outright, not relabeled); 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | supersedes prior status: the iter3/final-verify PASS (run-final-verify.md, main@720fb5f5) covered the *old* pre-remap arrow semantics — not valid evidence for this mapping; needs a fresh VM run before re-claiming Verified |
| cull-003-rating-label-flag-keys | cull-003-rating-label-flag-keys.md — The full keyboard decision vocabulary — ratings, color labels, flags — each its own undo step and toast | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; Marcus (persona-7) blasted 30 keys at 80ms — 30/30 landed, catalog agreed byte-for-byte; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | one eaten key seen in a later 49x blast at 90ms (persona-7) — watch, not blocking; supersedes prior status: no substantive change, noted for the record per house style |
| cull-004-stack-promote-return | cull-004-stack-promote-return.md — Return promotes one frame and rejects every sibling as one atomic gesture; no-op without stack membership | Reconciled — NOT re-run | VM e2e | — | PENDING-VM batch re-run PASS on burst: promote protects existing picks (12 stayed pick), verbatim toast, single ⌘Z reverts only the promote's writes; reconciled 2026-08-09 (Task 13, unified-shell push): rewrote the investigation section's premise from `beginStackCullingFromLatestImportCompletion()`/the deleted post-import banner to `beginStackCulling(importSessionID:title:)`, reachable from the sidebar import row's context menu | final-verify: Return-promote verified on real burst; rail Keep no-op root-caused (membership partition mismatch) and fixed c176c491, rail press live-verified writing pick+rejects; supersedes prior status: the premise describing how a persisted stack gets created no longer compiles — needs a fresh VM run |
| cull-005-scope-cycle | cull-005-scope-cycle.md — S cycles Unrated→Picks→Rejects→All, narrowing the grid each time, with nearest-match reselection | Reconciled — NOT re-run | VM e2e (ax+sql) | UX | Dana (persona-8): S is a silent mode change — filmstrip renumbers, no toast names the new scope; Marcus: "Unrated" really means undecided flag, and an empty scope renders a blank loupe with no scope label; scope-change toast fixed a077e12c (9aadec94); 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only; found but NOT fixed 2026-08-09 (Task 13 review follow-up): Step 4 cites the dead `CullFilmstripPresentation.positionText` (a pre-unified-shell "run strip" redesign replaced it with `tripleCounterText` in a different format) — see the card's own note; its "or by counting visible tiles" fallback is unaffected | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: no substantive change, noted for the record per house style; Step 4's filmstrip-format sub-claim now needs the same dedicated audit `cull-013` needs before its next VM run |
| cull-006-zoom-and-face-zoom | cull-006-zoom-and-face-zoom.md — 1:1 zoom toggle and face-zoom cycling in the loupe | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | screenshot-diff method now runnable; card itself needs a fresh run; supersedes prior status: no substantive change, noted for the record per house style |
| cull-007-exif-overlay-cycle | cull-007-exif-overlay-cycle.md — I cycles the loupe's EXIF overlay through off/exposure/full | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | needs fresh full card re-run to Verify; supersedes prior status: no substantive change, noted for the record per house style |
| cull-008-subview-keys-gcb | cull-008-subview-keys-gcb.md — G/C/B switch Cull subviews; grid Return/G/Esc jump to and from the loupe | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged, and fixed a stale `CullingKeyCaptureGate.isActive(workspace:...)` citation (renamed `lens:` by cull-001's rewrite) found while verifying | needs fresh full card re-run to Verify; supersedes prior status: this card's own citation was stale independent of the LEDGER's PASS evidence, which is otherwise unaffected — needs a fresh VM run |
| cull-009-keymap-overlay | cull-009-keymap-overlay.md — ? shows the full cull keyboard cheat-sheet | Reconciled — NOT re-run | VM e2e (ax+sql) | UX | reconciled 2026-07-13 (cull-stack-rail branch): removed the deleted `isMonitorOnly`/⌥-arrow rows ("Previous/Next Stack (Option)", "⌥←"/"⌥→" — the mechanism itself was deleted), rewrote the Navigation-section assertions to the current advertised set, added the new ↑/↓ overlay-scroll leg; also re-verified every line-number citation, several of which had drifted ~670 lines as the file grew; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1/⌘2 preamble unchanged in effect and fixed a stale `workspace ==` predicate citation in Sharp edges | supersedes prior status: the prior "Verified" run covered overlay content that included the now-deleted Option-arrow rows — not valid evidence for this revision; needs a fresh VM run |
| cull-010-cullgrid-keys | cull-010-cullgrid-keys.md — cull grid arrow/Home/End navigation and batch rating in one keystroke | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | batch-rating steps now runnable; card needs a fresh run; supersedes prior status: no substantive change, noted for the record per house style |
| cull-011-hud | cull-011-hud.md — The Cull HUD's progressive-disclosure cluster and verdict fallback | Reconciled — NOT re-run | VM e2e (ax+sql) | — | HUD math held live for Marcus (37/40/53 agreed with catalog byte-for-byte); PENDING-VM batch verified the hover-reveal loupe controls live (appear on movement, idle-hide ~1.5s, keystroke-hide, hover-Pick click writes flag via SQL); hover-hint surface then changed by 44793239 (9aadec94) without a fresh drive; 2026-08-09 (Task 13, unified-shell push) repointed the Pre-state's `workspace-switching.md` convention citation to `app-019-lens-shell.md` (⌘1's effect unchanged) | HUD matrix PASSed PENDING-VM; hint-surface change re-verified live post-fix b65b4e7a; supersedes prior status: citation-only, the PASS evidence is unaffected |
| cull-012-closeups-panel | cull-012-closeups-panel.md — Close-Ups face-crop rail is Cull-chrome-only and writes nothing | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter3 post-fix re-run PASS; single-fire confirmed via SQL deltas; reconciled 2026-08-09 (Task 13, unified-shell push, load-bearing): the ⌘1/⌘2 round trip is this card's actual test (proving the rail is Cull-chrome-only) — a mechanical ⌘2 swap would land in the Grid lens (no loupe at all), so Step 3 was rewritten to ⌘3 (the Loupe lens's `.libraryLoupe`), the true like-for-like successor to the old Library workspace's loupe; also fixed the gate's drifted line citation (`:3903` → `:3796`) | open q: Close-Ups re-detects live vs face_observations table — counts may disagree; intended?; supersedes prior status: every prior live run drove the old ⌘2-into-Library-loupe round trip, which no longer exists — needs a fresh VM run |
| cull-013-filmstrip | cull-013-filmstrip.md — The Cull filmstrip is scope-filtered, shows stack position, and tiles carry decision badges | Reconciled — NOT re-run | VM e2e | Functional | Marcus (persona-7): filmstrip said "frame 1 / 120" while the header said 130 (page-window numbering leak); catalog-wide filmstrip frame numbers fixed fa2c112c (22861c84); reconciled 2026-08-09 (Task 13 review follow-up, unified-shell push): fixed a `CullSidebarView` orphan citation (→ the sidebar's Cull-lens-only Stacks section) and an adjacent drifted line number; found but explicitly NOT fixed — the whole card describes a flat filmstrip (`cullingFilmstrip`/`filmstripTile`/`positionText`) that a separate, larger "run strip" redesign (Task 6, predating unified-shell) already replaced in `Sources/`, per the card's own new Run-status note | supersedes prior status: "Verified"/"final-verify run PASS" describes the flat filmstrip this push's Sources no longer implement, and already disagreed with the card's own "UNRUN" Run-status line before this fix; card needs a full rewrite against the current run-strip code, then a fresh VM run |
| cull-014-stack-rail | cull-014-stack-rail.md — Stack rail's primary Keep button and its secondary-actions ellipsis menu | Reconciled — NOT re-run | VM e2e | — | reconciled 2026-07-13 (cull-stack-rail branch) to the vertical thumbnail rail (per-frame thumbnail cells replacing text chips, footer-placed Keep/ellipsis menu, one AI-read badge per flaw kind replacing the single red dot); all line-number citations re-verified against a much-grown file; 2026-08-06: its `AutopilotProposalKind` citation-shift note now says the partition was deleted outright by SP-D0, not merely re-shifted | supersedes prior status: the Task-12 PASS predates both the visual reorg and the line-number drift — not valid evidence for this revision; needs a fresh VM run |
| cull-015-sidebar-sources | cull-015-sidebar-sources.md — Cull sidebar's Smart Collections section (zero-count omission, AI Suggestions/Analysis Failures rows) | Reconciled — NOT re-run | VM e2e (ax+sql) | Functional | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); `.autopilotProposals` presence predicate is now `!autopilotGhostAssetIDs.isEmpty`; reconciled 2026-08-09 (Task 13, unified-shell push): full rewrite to the Smart Collections contract — the "Cull From" concept and `CullSidebarView` are gone, zero-count rows omitted (not disabled), `AI Suggestions` gated on `autopilotGhostAssetIDs`, `Analysis Failures` as the tenth row, saved dynamic sets joining the section, "Nothing to cull" replaced by section-absence | supersedes prior status: the final-verify PASS predates both `pendingAutopilotProposals`'s deletion and the Smart Collections rewrite — not valid evidence for the current predicate or section; needs a fresh VM run |
| cull-016-completion-stage | cull-016-completion-stage.md — deciding all frames shows the end-of-set handoff, Review Picks/Move Rejects/dismiss all work | Reconciled — NOT re-run | VM e2e (ax+sql) | — | reconciled 2026-08-06: now the **authoritative** post-drop completion action set (five titles — Export/Move Rejects…/Move Rejects to Trash…/Review Picks/Save Picks as Set — verified against `CullCompletionPresentation.swift`/`LibraryGridView.swift`), cross-referenced from cull-025 so the two no longer conflict; prior run found completion/Review Picks/scope reappearance correct, counts summed exactly (37+53=90); 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | adopts end-of-set-move-rejects; items 49-51 (session banners) blocked by stack fixture gap; wart (persona-7): stale import status concatenated into the completion subtitle; supersedes prior status: the prior 3-action assertion was already stale before this task — not valid evidence for the five-action set; needs a fresh VM run |
| cull-017-autopilot-review | cull-017-autopilot-review.md — Autopilot proposes, badges the grid, you review (selected/all), commit, dismiss, undo | Reconciled — NOT re-run | VM e2e | Testability | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); rewritten wholesale to the `metadata_json` ghost query; prior BLOCKED-TOOLING attempt needs host fixture gen + submit_import_path equivalent in VM regardless; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged and fixed a live "In Library workspace" reference (Step 4) to "In the Grid lens"; 2026-08-09 (Task 13 review fix, second pass) found the prior sweep had marked the card fully reconciled while Step 6 still cited three dead symbols (`cullSourcePresentation`, `CullSource.Target.autopilotProposals`, `activateCullSource`), a bogus line citation, and a dead AX label ("Autopilot Proposals") the live UI no longer renders — rewrote Step 6 to the real `selectSidebarRow(_:)` → `selectSource(_:)` → `applyAutopilotSuggestionsScope()` chain and the real "AI Suggestions" label, and fixed five more line citations elsewhere in the card that had drifted 87-112 lines | adopts autopilot-review-commit-undo; open-q resolved — Review is reachable after Dismiss via the sidebar's "AI Suggestions" row (`LibrarySource.autopilotSuggestions`), not the "Cull sidebar's Autopilot Proposals source" this row previously and incorrectly said; supersedes prior status: BLOCKED-TOOLING attempt predates the table drop — needs a fresh VM run |
| cull-018-compare-survey | cull-018-compare-survey.md — Compare survey — header, contenders toggle, badges/metrics, group actions, reject-refill, and the shared key monitor | Reconciled — NOT re-run | VM e2e (ax+sql) | — | core verified; worker-eval steps (Evaluate Compare/contenders/refill) unexercised — needs evaluated fixture; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged and fixed a stale `CullingKeyCaptureGate.isActive`'s `workspace ==` predicate citation in Step 8's PROBE | open q: shared monitor key semantics in compare; CONFIRMED UX inconsistency: Return uses stricter stack-guard and can silently no-op while Keep-primary button is enabled; supersedes prior status: the citation was stale independent of the core-verified evidence, which is otherwise unaffected — needs a fresh VM run |
| cull-019-ab-compare | cull-019-ab-compare.md — A/B head-to-head — panes, contender resolution, synced zoom, keep-decision write | Reconciled — NOT re-run | VM e2e (ax+sql) | — | header/contender/keep-write verified via SQL; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | supersedes prior status: no substantive change, noted for the record per house style |
| cull-020-pass-scope-and-undo | cull-020-pass-scope-and-undo.md — P/X/S/Return in the Cull workspace, and ⌘Z reverts one gesture at a time | Verified | VM e2e | — | stack leg PASS on burst; full card green; Marcus re-exercised the P/X/⌘Z loop live at speed with exact catalog agreement | adopts cull-pass-scope-and-undo; S-scope UX defect tracked on cull-005, not here |
| cull-021-stack-rail-nav | cull-021-stack-rail-nav.md — The vertical current-stack rail: within-stack ↓/↑, across-stack ←/→ landing on the recommended frame, click-to-loupe, and Return promote/reject | Spec'd | VM e2e (ax+sql) | — | new card authored 2026-07-13 for the cull-stack-rail branch (Task 6); source-cited, not yet driven | pending live VM run; documents an honest fixture gap — the `burst` seed's synthetic frames carry no evaluation signals until Evaluate is triggered, and may never cross the flaw-badge thresholds (no faces, unproven focus scores) |
| cull-022-flow-grammar-walk | cull-022-flow-grammar-walk.md — H/L/J/K (+ arrows) walk burst and no-burst batches with identical grammar, ✦-or-first landing, and the `A` auto-advance toggle | Reconciled — NOT re-run | VM e2e (ax+sql) | Documentation | RUN 2026-07-28, app 878f1939 (merged main): PASS-WITH-CARD-FIXES on both `burst`/`smoke` legs, two documentation-drift card bugs found and fixed (unsound multi-frame-vs-standalone probe; two-way landing branch corrected to three-way), no app bugs; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged and fixed a stale `CullingKeyCaptureGate.isActive`'s `workspace ==` predicate citation | added to LEDGER 2026-08-09 (was previously untracked); supersedes prior status: the 2026-07-28 PASS-WITH-CARD-FIXES evidence is unaffected by the citation fix — needs a fresh VM run regardless, per its own dated entry |
| cull-023-return-commit-undo | cull-023-return-commit-undo.md — Return commits a stack — pick the staged frame, reject undecided siblings, protect prior picks, force-flip a rejected frame with disclosure, and undo the whole gesture in one step | Reconciled — NOT re-run | VM e2e | — | PASS-WITH-CARD-FIXES — run live 2026-07-28 against app build 878f1939 in the teststrip-e2e Tart VM (`burst` fixture); Steps 1-6 PASS on toast text and SQL/sidecar ground truth after three card bugs fixed; Step 7 (armed-commit leg) confirmed not executable against this fixture (see `cull-027-blaze-through-prefetch.md`); re-verified 2026-07-30 against SP-C's armed-commit rewrite (render-gate citations only); 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘1 preamble unchanged in effect, preamble only | added to LEDGER 2026-08-09 (was previously untracked); supersedes prior status: no substantive change from the preamble check, noted for the record per house style |
| lib-001-sidebar-sections | lib-001-sidebar-sections.md — Library sidebar sections render in fixed order with correct rows, badges, and disabled state | Verified | VM e2e (ax+sql) | — | iter1 PASS; PENDING-VM batch re-ran the changed empty-state surface post-8bc5f704: filtered-to-zero copy ("No photos match these filters" + Clear Filters) and post-submit focus both PASS live | Dana's cold-open empty states also read well (persona-8) |
| lib-002-saved-set-context-menus | lib-002-saved-set-context-menus.md — sidebar row context menus expose the correct per-row-type actions and write on confirm | Verified | VM e2e | — | iter3 re-run PASS post right-click fix (work-session leg vacuous on smoke) | note: work-session menu is a single star-toggle whose title flips; possible dup SidebarRow.id across sections (List diffing footgun) |
| lib-003-token-grammar-fields | lib-003-token-grammar-fields.md — the structured `field:value` token grammar narrows the grid per its documented SQL semantics | Tested-Fail | VM e2e | UX | unquoted multi-word value silently drops trailing words (camera:SmokeCam 1 → 24 not 8); quoted works — Jesse decision: auto-quote/greedy-consume or document-only |  |
| lib-004-bare-and-phrase-tokens | lib-004-bare-and-phrase-tokens.md — bare flag/rating words and multi-word phrase predicates narrow the grid | Tested-Pass | VM e2e (ax+sql) | — | iter1 PASS |  |
| lib-005-token-readback-roundtrip | lib-005-token-readback-roundtrip.md — structured filter state round-trips through query token text | Tested-Pass | unit + VM spot-check | — | unit pass; AX spot-check pass except card drift (expected 10 vs actual 9 chips) — card fix queued | all round-trip + grammar assertions pass; AX spot-check pending VM batch |
| lib-006-query-field-and-tips | lib-006-query-field-and-tips.md — the query field's icons drive parse+submit, and the tips popover lists the token groups | Reconciled — NOT re-run | VM e2e (ax+sql) | — | PENDING-VM batch: post-submit focus returns to grid PASS, save-confirmation auto-clear PASS; Esc two-stage leg FAIL/inconclusive live (banner cleared before Esc in 3 runs — staged flow not observable); root-caused (field bound committed state) and fixed 86c34750 via search draft (a4802c75); unit-tested; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): ⌘2 preamble "Library" → "Grid lens"; `requestFocusSearch()` no longer calls deleted `selectWorkspace(.library)` — it calls `selectLens(.grid)`; `CullingKeyCaptureGate` reworded "workspace `.cull`" → "lens `.cull`" | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: the PASS predates the workspace→lens rewrite and the citation was pointing at code that no longer compiles — needs a fresh VM run |
| lib-007-add-filter-menu | lib-007-add-filter-menu.md — the "Add filter" menu covers enum fields with fixed option lists; free-text fields stay typed-only | Verified | VM e2e (ax+sql) | — | Task-12 re-run PASS |  |
| lib-008-chips-remove-clear | lib-008-chips-remove-clear.md — filter chips dedupe by identity, removal is per-property, and "clear all" only shows when a filter is active | Reconciled — NOT re-run | VM e2e (ax+sql) | UX | iter1 PASS; chip-copy surface then changed in persona-8 fix round (9aadec94; Dana: suggestion chips read as commands with no object — "Apply adult"; chip soup accumulates) without a fresh drive; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): fixed a stale `SidebarRowTarget` chip-identity citation (deleted; `ActiveLibraryFilterRow.target` is `LibrarySource?`) | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: no substantive change to any assertion, noted for the record per house style |
| lib-009-sort-and-bar-extras | lib-009-sort-and-bar-extras.md — the sort menu's 6 options, and the query bar's conditional sync-retry/refresh-source buttons | Verified | VM e2e (ax+sql) | — | Task-12 re-run PASS (default-sort checkmark AX-unverifiable, noted); PENDING-VM batch: sort-tooltip leg PASS live (label names current order) | |
| lib-010-result-header-save | lib-010-result-header-save.md — result count, plain-text interpretation, catalog-backed suggested chips (active-field suppressed), and the 3-way Save menu | Verified | VM e2e (ax+sql) | UX | Task-12 re-run PASSed, but Dana (persona-8) couldn't parse the plain-text fallback ("Plain search fallback" chip, result set visibly unchanged, no "0 matches" moment); fallback copy fixed 1a3322c5 (9aadec94) | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| lib-011-view-toggle-routing | lib-011-view-toggle-routing.md — the six-segment lens switcher routes the body to the correct lens, and the Cull-lens g/c/b sub-mode routes | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter1 PASS; reconciled 2026-08-09 (Task 13, unified-shell push): rewritten wholesale — the five-tag `librarySubViewToggle`/`Picker("Library View", ...)` is gone, replaced by the six-segment `lensSwitcher`; `LibraryViewMode`'s remaining four cases are Cull-lens sub-modes reached by g/c/b, not switcher tags; added a new Step 9 for the sub-mode routes | supersedes prior status: the iter1 PASS covered a `Picker` control that no longer exists in source — not valid evidence for the switcher; needs a fresh VM run |
| lib-012-grid-keys | lib-012-grid-keys.md — grid keyboard navigation, rating shortcuts, and loupe transition | Reconciled — NOT re-run | VM e2e (ax+sql) | — | reconciled 2026-08-06: "unlike Autopilot proposals" became "unlike an autopilot ghost" (terminology only); iter1 PASS | supersedes prior status: no substantive change, noted for the record per house style |
| lib-013-library-loupe | lib-013-library-loupe.md — the Loupe lens (⌘3) is plain navigation, no pick/reject pills, and is session-restorable | Reconciled — NOT re-run | VM e2e (ax+sql) | — | iter1 PASS; reconciled 2026-08-09 (Task 13, unified-shell push): `.libraryLoupe` retitled from "the Library workspace's loupe" to "the Loupe lens," entry key ⌘2 → ⌘3, added a new Step 5 asserting session-restorability (`isRestorableLens` excludes only Cull) | adopts library-loupe-no-cull-chrome; supersedes prior status: the iter1 PASS covered the deleted Library workspace's ⌘2 route — not valid evidence for the Loupe lens's ⌘3 route or the new restorability assertion; needs a fresh VM run |
| lib-014-map-clusters-scoping | lib-014-map-clusters-scoping.md — GPS photos cluster on the map and reverse-geocode to names, scoped to the active query | Tested-Pass | VM e2e | Environment | clusters+query-scoping PASS; geocode legs blocked (CLGeocoder unreachable from VM) — SKIP-offline honored | adopts places-map-and-geocode; verify 62e0a31 query scoping; CLGeocoder hang in VM now root-caused on worker-006 |
| lib-015-timeline | lib-015-timeline.md — year ribbon / scrubber / sections track the loaded assets and drive scroll-to-focus | Tested-Pass | VM e2e (ax+sql) | — | iter1 PASS |  |
| lib-016-grid-badges | lib-016-grid-badges.md — thumbnail chrome — selection borders, metadata overlay, availability, batch/autopilot badges, preview status | Reconciled — NOT re-run | VM e2e | — | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); autopilot badge Source bullet re-anchored on `AutopilotGhost.kind(in:)`, deleted the `.keyword`-proposal case | supersedes prior status: the iter3 a11y-fix PASS predates the ghost-derivation model — not valid evidence for the badge's current source; needs a fresh VM run |
| lib-017-footer-density-zoom | lib-017-footer-density-zoom.md — footer counts, selection-clear, density presets, and zoom slider | Tested-Pass | VM e2e (ax+sql) | — | iter1 PASS |  |
| lib-018-pagination | lib-018-pagination.md — Load Previous / Load More paging controls | Tested-Pass | VM e2e | Testability | reachable legs PASS on smokebig; Load Previous needs >240 fixture | BLOCKED fixture gap: assetPageSize=120 vs 24 smoke assets — pagination unreachable with current seeds |
| lib-019-multiselect | lib-019-multiselect.md — click / ⌘-click / ⇧-click selection semantics and focus policy | Tested-Pass | VM e2e (ax+sql) | — | iter1 PASS |  |
| lib-020-token-query-filter | lib-020-token-query-filter.md — typing a filter token narrows the grid to the matching catalog rows | Verified | VM e2e (ax+sql) | — | iter1 PASS; Priya (persona-6) re-exercised live on faces seed: rating:4 → "Rating >= 4" chip, keyword:"…" (case-insensitive), person:"…" all matched sqlite3 ground truth; unknown tokens honestly banner as plain text | adopts token-query-filter |
| people-001-canvas-header | people-001-canvas-header.md — People canvas three-panel layout and the header count | Reconciled — NOT re-run | VM e2e | UX | earlier PASS; Dana (persona-8): empty state speaks developer ("populate local face review queues", deferred-feature release-notes text in a first-run screen); copy fixed cecbcc61 (9aadec94); reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): ⌘3-for-People preambles (Steps 2, 6) fixed to ⌘6 — People is a top-level `LibraryLens` case now, not the deleted three-case `Workspace`'s ⌘3 | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: the PASS predates the lens rewrite — the key pressed no longer opens People — needs a fresh VM run |
| people-002-suggestion-cards | people-002-suggestion-cards.md — Naming a face group writes only on confirm | Verified | VM e2e | — | isolated re-run PASS post attach-ruling; Dana (persona-8, post-aed1dbbe) re-verified live: people/person_assets 0 rows until Create Person, then Riley + 11 rows; group-naming dialog now shows face/photo counts | adopts people-name-face-group-happy-path |
| people-003-cluster-identity | people-003-cluster-identity.md — Automatic grouping clusters by face identity | Tested-Pass | VM e2e | — | PASS; tap sub-leg blocked (no click verb) | adopts people-cluster-by-identity |
| people-004-review-cards | people-004-review-cards.md — "Unnamed faces" / "Face quality checks" review cards route and gate correctly | Reconciled — NOT re-run | VM e2e | — | PASS; disabled-state leg unreachable per card; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): preamble fixed from a Library sub-view toggle to ⌘6/the lens switcher's People segment; fixed a dead `selectSidebarTarget` citation — routing is now `model.selectSource(_:)` on the card's `target: LibrarySource?` | supersedes prior status: the PASS predates both the lens rewrite and the routing-mechanism rename — needs a fresh VM run |
| people-005-queue-keyboard | people-005-queue-keyboard.md — arrow to a card, Return confirms — person_assets appears only after | Reconciled — NOT re-run | VM e2e | — | isolated re-run PASS; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): ⌘3-for-People preamble fixed to ⌘6 | adopts people-confirm-writes-on-return; supersedes prior status: the PASS predates the lens rewrite — the key pressed no longer opens People — needs a fresh VM run |
| people-006-sheet-return-routing | people-006-sheet-return-routing.md — Return with the naming sheet open triggers Create, not the queue confirm | Reconciled — NOT re-run | VM e2e | — | isolated re-run PASS; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): ⌘3-for-People preamble fixed to ⌘6 | adopts people-naming-sheet-return-routing; supersedes prior status: the PASS predates the lens rewrite — the key pressed no longer opens People — needs a fresh VM run |
| people-007-name-selection | people-007-name-selection.md — Name Selection sheet writes only on submit; Dismiss writes nothing | Verified | VM e2e | UX | isolated re-run had PASSed and Priya (persona-6) re-confirmed confirm-before-write at the SQL level mid-gesture — but the blind dialog (no count/thumbnails of the selection) let her file "Sally K. Ride" onto an Aldrin photo; naming-count fix aed1dbbe | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| people-008-person-cards-merge | people-008-person-cards-merge.md — confirmed person cards, merge, navigation, and the duplicate-name probe | Reconciled — NOT re-run | VM e2e | Testability | merge/counts PASS; card-body tap blocked (no CGEvent click verb); dup-name probe confirmed dup rows — since FIXED by 93415485, re-verify; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): Step 2's preamble ("⌘2 Library then AX-press the sub-view toggle") fixed to ⌘6/the lens switcher's People segment | open q: duplicate-name people; open q: duplicate-name people minted silently (people.name non-unique, no match-existing step) — product gap for Jesse; supersedes prior status: the PASS predates the lens rewrite — needs a fresh VM run |
| people-009-scan | people-009-scan.md — "Scan for Faces" is menu-only, gated on worker+catalog+cached previews, and surfaces in Activity | Verified | VM e2e | Functional | earlier PASS, but Priya (persona-6, faces seed): scan never started, silently — 4+ min polling, face_observations 0, zero face jobs enqueued, banner stuck at "Scan ready" (suspect stale source_roots pointing at the seed-building machine); offline-sources status surfaced by aed1dbbe | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| inspect-001-toggle-tabs | inspect-001-toggle-tabs.md — ⌘I toggle, ⌥⌘1-3 scroll-to-section, and the elementsByTab anti-orphan mapping | Reconciled — NOT re-run | VM e2e | — | reconciled 2026-07-13 (feat/unified-single-view branch, Task 6/9) to the stacked-sections inspector: segmented-Picker tab switching removed (all four sections — Info/Describe/AI/People — render at once in a plain, non-lazy VStack), ⌥⌘1-3 now scroll-to-section instead of switching a selected tab, and Cull's old ⌘I-switches-to-Library special case is gone (`showsInspector` is unconditionally true, `toggleInspector` is a bare boolean toggle); reconciled 2026-08-09 (Task 13, unified-shell push): noted ⌥⌘1-3 unchanged, now sitting beneath the new ⌘1-⌘6 lens keys; fixed a live wrong claim ("Cull is the default workspace on a fresh launch" — actually Grid) and two drifted symbol/line citations (`WorkspaceChromePolicy`→`LensChromePolicy`, `scrollInspector` line range) | supersedes prior status: the earlier "PASS; width/no-selection legs unmeasurable in VM" covered the *old* tabbed-Picker UX only — not valid evidence for this revision; needs a fresh VM run; deep People-section coverage (naming/reject/remove/box overlay) moved to inspect-010-photo-faces, not duplicated here |
| inspect-002-info-identity-exif | inspect-002-info-identity-exif.md — Info tab preview, identity header, read-only summary, and conditional EXIF rows | Tested-Pass | VM e2e | — | PASS; summary child text not AX-exposed (note) | fixture gap: smoke seeds lack aperture/shutter/focal-length |
| inspect-003-sync-status-conflict-resolver | inspect-003-sync-status-conflict-resolver.md — Sync status priority and the three conflict resolutions | Verified | VM e2e | Functional | retry drains post-fixes; VM live run + 2-layer regression tests; recurrence lead: async IPC ordering | conflict trigger inferred (fingerprint mismatch) — confirm on first live run |
| inspect-004-retry-surfaces | inspect-004-retry-surfaces.md — Preview-retry and provider-failure retry actually re-queue work | Reconciled — NOT re-run | VM e2e | Functional | tolerant decode PASS; provider-retry full PASS; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘2 preamble ("Library" → "Grid lens") is preamble-only, unaffected by which lens the inspector opens over | failure rows synthesized via SQL — proves retry wiring, not failure detection; supersedes prior status: no substantive change, noted for the record per house style |
| inspect-005-describe-editing | inspect-005-describe-editing.md — Describe tab editing controls, draft protection, and batch text-field apply | Verified | VM e2e | — | batch text fields live PASS; blur-does-not-commit documented; Priya (persona-6) re-exercised captions/keywords live — every field landed in catalog + well-formed sidecar XMP | open q: batch vs single-asset asymmetry; confirmed asymmetry: rating/flag/label batch vs keywords/caption/etc single-asset, note only near batch controls — UX question for Jesse; no keyword vocabulary support (autocomplete/browser) — Priya's top-5 |
| inspect-006-suggested-keywords-ocr | inspect-006-suggested-keywords-ocr.md — suggested-keyword chip accept writes catalog + sidecar; OCR captions | Tested-Fail | VM e2e | Testability | smoke lacks keyword signals (card fixture bug); faces chips over budget | adopts inspector-describe-suggested-keyword |
| inspect-007-ai-verdicts | inspect-007-ai-verdicts.md — AI tab's plain-English verdict groups and technical disclosure | Tested-Pass | VM e2e | — | PASS |  |
| inspect-008-sidecar-write-semantics | inspect-008-sidecar-write-semantics.md — every edit writes catalog first, then sidecar; worker presence changes sync timing | Verified | VM e2e | Functional | earlier PASS and Priya verified row-for-row rating→sidecar fidelity (11/11), but Dana (persona-8) caught browsing writing Rating=0 sidecars for merely-viewed photos — violates the no-sidecar-from-browsing invariant; fixed 7b35c739 (9aadec94, MetadataSyncPlanner) | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| inspect-009-metadata-undo | inspect-009-metadata-undo.md — ⌘Z/⇧⌘Z on metadata edits use a dedicated undo stack with labeled change groups | Tested-Pass | VM e2e | — | PASS |  |
| inspect-010-photo-faces | inspect-010-photo-faces.md — the People inspector section lists per-photo faces, names/rejects/removes write only on the gesture, and boxes track the loupe image | Reconciled — NOT re-run | VM e2e (ax+sql) | — | new card authored 2026-07-13 for Task 9 (feat/unified-single-view branch); source-cited, not yet driven; reconciled 2026-08-09 (Task 13, unified-shell push): every "workspace" reference described the deleted three-case `Workspace` enum — `WorkspaceChromePolicy`→`LensChromePolicy`, `.workspace`→`.lens`, ⌘2 preambles "Library"→"Grid lens", plus two drifted `toggleInspector`/`openAssetInLoupe` line citations | pending live VM run; hard-blocked on the AuraFace model actually being downloaded (dev-008's fixture-gap risk — manifest URL may not resolve) since `face_observations` is empty without it; flags an open product question — Remove (`unassignFaces`) never clears the `person_assets` row `assignFaces` wrote, so a once-confirmed photo is permanently excluded from future face suggestions even after its name is removed; supersedes prior status: no prior run evidence exists to invalidate (still Spec'd/NOT RUN) |
| import-001-folder-in-place | import-001-folder-in-place.md — Import Folder never touches the original bytes, and fresh assets come online | Verified | VM e2e | — | PASS — originals untouched (checksums); Dana (persona-8, 22861c84) re-exercised live: 28 DNGs cataloged in place, thumbnails in seconds, zero .xmp written at import, catalog rows match | |
| import-002-card-copy | import-002-card-copy.md — Import Card copies into managed storage, dedupes in-batch, and refuses to silently overwrite a collision | Tested-Pass | VM e2e | — | PASS | open q: per-file skip vs whole-batch failure on collision — card accepts either, confirm live |
| import-003-menu-and-dev-path | import-003-menu-and-dev-path.md — File menu exposes Import Folder/Card/Path as distinct items, mirrored by the toolbar, both disabled mid-import | Tested-Pass | VM e2e | — | PASS |  |
| import-004-new-only-dedupe | import-004-new-only-dedupe.md — Re-importing a card copies only new frames | Verified | VM e2e | Functional | dedupe itself held live for Marcus (re-import of 90 cataloged files added nothing, "90 photos already in catalog"), but the preflight claimed "Duplicates: 90 new" — a lie that would scare users into cancelling; fixed ae565378 (22861c84) | fix 41df4009: honest dedupe-off contract (UNIQUE original_path — re-import in place); preflight strings live-verified |
| import-005-sidecar-on-import | import-005-sidecar-on-import.md — importing alongside .xmp sidecars folds, conflicts, or isolates a bad file — never aborts the batch | Tested-Fail | VM e2e | Testability | a/c PASS; b: planner conflict branch unreachable on re-import (re-sync semantics) — card expectation fix queued; corroborates no-rescan gap (now addressed on activity-006) | first-import conflict unreachable (planner folds unconditionally); second-import framing used; Priya: pre-existing sidecar values ignored at import, lazily adopted on first touch — product question for Jesse |
| import-006-availability-badges | import-006-availability-badges.md — Source availability states badge distinctly and gate full-res access | Tested-Pass | VM e2e | — | PASS |  |
| import-007-refresh-reconnect | import-007-refresh-reconnect.md — Refresh availability vs. Reconnect rewrite root+bookmark+previews | Tested-Pass | VM e2e | Environment | PASS core; repair-row leg blocked-env; sourceScan rows never persist (observability gap) |  |
| import-008-auto-cull-toggle | import-008-auto-cull-toggle.md — Autopilot-after-import proposes over exactly the imported set, provisionally | Reconciled — NOT re-run | VM e2e | Functional | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); baseline/scope/status queries became `metadata_json` ghost queries, commit step now routes through the Cull sidebar | supersedes prior status: the armed-autopilot live-verified PASS predates the table drop — not valid evidence for the rewritten queries; needs a fresh VM run |
| import-009-cull-pick-journey | import-009-cull-pick-journey.md — A first session, import to picks | Reconciled — NOT re-run | VM e2e | — | journey PASS; Dana (persona-8, 22861c84) re-ran the full journey live — import 28 → cull → 4 picks → export 4 JPEGs in ~30 min, relaunch state intact; reconciled 2026-08-09 (Task 13, unified-shell push): Steps 2-3's deleted-banner AX elements ("Start culling"/"Review imported frames") replaced with the toast's "Start culling"; "review without culling" now lives as "Manual Compare over the import" in the sidebar import row's context menu | adopts import-cull-pick-happy-path (cross-area journey); not promoted: the sidecar-spray defect (inspect-008) surfaced mid-journey and its fix (9aadec94) postdates the run; supersedes prior status: the PASS predates the banner's deletion — the AX elements it drove no longer exist — needs a fresh VM run |
| import-010-import-robustness | import-010-import-robustness.md — large card imports survive slow copies, refuse a full disk, and show real counts | Tested-Pass | unit + targeted | — | watchdog heartbeat, free-space preflight, and real preview count all unit-green (solo suite 1911/0); live count leg PENDING-VM | watchdog misfire & preflight not hermetically live-drivable — unit-covered; follow-ups: 2nd-copy same-volume preflight edge, ImportDedupPreview.scan still numeric-capped |
| activity-001-icon-states | activity-001-icon-states.md — toolbar Activity icon states (idle/working/problem) and popover-row-click navigation | Tested-Pass | VM e2e | — | PASS | adopts activity-icon-states + quiet-activity-badge; known gap: no UI rescan trigger — now addressed by sidecar rescan (cfd9d9d0), re-check card |
| activity-002-popover-import | activity-002-popover-import.md — Activity popover shows live import phase/progress with cancel, surfaces errors, and (with the banner gone) is the sole durable receipt | Reconciled — NOT re-run | VM e2e | Testability | import-row window closes before AX walk under VM churn; reconciled 2026-08-09 (Task 13, unified-shell push): strengthened per brief — added the "Recent Imports" receipt section, its "Start culling" action, the 5-item retention limit, and the receipts-never-badge invariant; executable Step 9 reconciled 2026-08-10 to the receipt's live-proven `AXLink` role and current Activity Center anchors | `AXLink` is cross-card evidence from the separate `import-011` VM run; `activity-002` itself was NOT re-run. Suspected bug: completed-with-errors import surfacing gap in popover; the old Tested-Fail predates Part C — needs a fresh VM run |
| activity-003-jobs-controls | activity-003-jobs-controls.md — Activity popover jobs list caps at 4 and exposes per-job controls | Tested-Fail | VM e2e | Testability | no sustainable job rows on smoke; popover render screenshot-proven | fixture gap: >4 concurrent jobs may not be exercisable (smoke drains fast) |
| activity-004-sources-conflicts-quiet | activity-004-sources-conflicts-quiet.md — Activity popover Sources refresh/reconnect, XMP Conflicts rows, and the quiet "No active work" state | Tested-Fail | VM e2e | Testability | quiet-floor + repair rows remain unexercised; 2026-08-10 contract cleanup re-anchored current Activity Center/AppModel symbols and clarified that `No active work` excludes active/problem sections under the precondition while durable `Recent Imports` receipts may coexist | NOT RUN: documentation/current-contract reconciliation only, not a passed VM leg; fresh VM run still required |
| activity-005-conflict-deep-link | activity-005-conflict-deep-link.md — clicking a conflict row deep-links into the Grid lens; ⌘⇧0 only toggles the popover | Reconciled — NOT re-run | VM e2e | — | conflict deep-link fully verified; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): every assertion was built on the deleted `Workspace` enum/`selectedWorkspace` — `revealConflicts` now sets `selectedView = .grid` and `selectedSource = .metadataSyncConflicts`; `inspectorTab == .info` replaced by a `scrollInspector(to:)` scroll target; stale ⌘3 preamble fixed to ⌘1/⌘6 | supersedes prior status: "conflict deep-link fully verified" was driven against a `selectedWorkspace` property that no longer exists — needs a fresh VM run |
| activity-006-xmp-lifecycle | activity-006-xmp-lifecycle.md — XMP conflicts arise from divergence or unreadable sidecars, and never auto-resolve | Verified | VM e2e | Functional | rescan path shipped (cfd9d9d0) and PENDING-VM batch verified both legs live on smoke (menu "Check Sidecars for Changes" detect→re-import 3→5; launch rescan 5→2) — but Priya (persona-6, faces seed) then hit a silent no-detect (sidecar edited on disk, catalog frozen, "XMP pending/conflicts: 0/0", zero feedback); whole-catalog rescan + completion report fixed in aed1dbbe | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| worker-001-preview-lifecycle | worker-001-preview-lifecycle.md — Preview badges track the worker from queued to built | Tested-Pass | VM e2e | — | PASS |  |
| worker-002-evaluation-verdicts | worker-002-evaluation-verdicts.md — Evaluation runs after preview caches, and re-scans don't double-enqueue | Reconciled — NOT re-run | VM e2e | — | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); Step 7's badge cross-check became a `metadata_json` ghost query; corrected a pre-existing "committed" wording bug in the intro | supersedes prior status: PASS predates the table drop; work_sessions observability gap unaffected — needs a fresh VM run |
| worker-003-face-pipeline | worker-003-face-pipeline.md — Re-processed assets replace face observations, then batch into suggestions | Tested-Pass | VM e2e | — | PASS — rescan replaces not appends | 2000-observation cap source-grounded only; no fixture reaches it |
| worker-004-death-recovery | worker-004-death-recovery.md — The queue recovers when the worker process dies mid-item | Tested-Pass | VM e2e | — | PASS — kill -9 → respawn ~5s, queue intact | persona-7 side-evidence: catalog survived three hard VM kills with zero corruption |
| worker-005-offline-reconnect | worker-005-offline-reconnect.md — The worker short-circuits offline sources and resumes after reconnect | Tested-Pass | VM e2e | — | PASS — no retry storm | open q: no confirmed UI trigger for post-reconnect availability re-probe |
| worker-006-geocode-backfill | worker-006-geocode-backfill.md — Geocode queue processes in throttled batches with a negative cache | Verified | VM e2e + unit | — | PENDING-VM 15-min live watch: CPU flat 0.0% across 16 samples (hot-loop fix holds), no tight requeue — but the queue row never reached terminal state: CLGeocoder hangs in the VM, the 120s supervisor timeout killed the worker before recordGeocodeFailure ran (slow perpetual retry); bounded 30s wait fixed 6736a714 (a4802c75) with unit+e2e tests, not yet re-run live | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| app-001-launch-scene | app-001-launch-scene.md — the app launches into its one-window scene, dark, titled, on the right catalog | Reconciled — NOT re-run | VM e2e | Functional | ⌘N no-op verified; tolerant-decode covers corrupt-catalog leg; PENDING-VM batch: window-title leg (catalogDisplayName + view subtitle) PASS live post-8bc5f704; reconciled 2026-08-09 (Task 13 review follow-up, unified-shell push): the intro's "sidebar swaps `CullSidebarView`/`SidebarView` by workspace" claim was a `CullSidebarView` orphan (deleted in this push) the sweep's grep pattern couldn't match on — corrected to the real one-sidebar structure with its Cull-lens-only Stacks section | supersedes prior status: the "Verified" status predates this push's sidebar consolidation and describes a swap this push deleted — needs a fresh VM run |
| app-002-window-floors | app-002-window-floors.md — one floor (`AppWindowLayoutMetrics.minimumWidth == 1000`) holds the chrome across every lens | Reconciled — NOT re-run | VM e2e | Testability | PARTIAL-tooling; reconciled 2026-08-09 (Task 13, unified-shell push): rewritten for the single 1000pt floor — deleted the Library-vs-Cull comparison and the People-rides-the-Library-floor note, rewrote steps to assert the same floor holds across lens switches instead of three separate floors | adopts workspace-minimum-width-floors; supersedes prior status: PARTIAL-tooling covered the old three-floor model, which no longer exists — needs a fresh VM run |
| app-003-workspace-switching | app-003-workspace-switching.md — **superseded**, now a stub pointer to `app-019-lens-shell.md` | Reconciled — NOT re-run | VM e2e | — | reviewed 2026-08-06 for ghost-derivation staleness (SP-D0 dropped `autopilot_proposals`) — no edit needed, card only names the literal "Autopilot Proposals" row label; PASS; Dana used ⌘1/⌘2/⌘3 throughout without incident; **replaced wholesale 2026-08-09 (Task 13, unified-shell push)**: the two/three-workspace `Workspace` enum and its ⌘1/⌘2/⌘3 bindings are deleted outright — the card is now a single-paragraph stub naming its successor, kept only so its LEDGER row and inbound cross-references (`app-019-lens-shell.md:56` cites its "switcher says Cull but ⌘1 lands on a view subtitled 'Loupe'" quirk) stay resolvable | minor (persona-8): switcher says Cull but ⌘1 lands on a view subtitled "Loupe" (documented in the stub, now also covered live by `app-019-lens-shell.md`'s Step 4); supersedes prior status: `Workspace`/`WorkspaceCommands`/`selectWorkspace` no longer exist in any form — the PASS evidence above describes a control surface that has been deleted; no further run of this card is possible or needed — see `app-019-lens-shell.md` |
| app-004-subview-menus | app-004-subview-menus.md — the View menu lists six lenses (⌘1-⌘6) then a divider then four Cull sub-modes | Reconciled — NOT re-run | VM e2e | — | PASS; reconciled 2026-08-09 (Task 13, unified-shell push): rewritten for the current View menu shape — 6 lens items + divider + 4 no-key-equivalent Cull sub-mode items; corrected the stale "People excluded" framing (People is now a lens item); `subViewMenuKey` → `cullSubModeMenuTitle`; added a new assertion for "Loupe" appearing twice (lens item and sub-mode-adjacent route) | supersedes prior status: the PASS covered the deleted five-tag sub-view Picker menu shape — not valid evidence for the six-lens menu; needs a fresh VM run |
| app-005-chrome-policy | app-005-chrome-policy.md — the simplified `LensChromePolicy` chrome policy is live in the assembled UI | Reconciled — NOT re-run | VM e2e | Documentation | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`) — "proposal batch is pending" became "while ghosts exist"; pre-existing script/card drift (stale Review row + Potential Picks breadcrumb in verify_ux_simplification_chrome.sh) unaffected, fix still queued; reconciled 2026-08-09 (Task 13, unified-shell push): `WorkspaceChromePolicy`→`LensChromePolicy`; deleted the false "⌘I switches to Library first" claim (`showsInspector` is unconditionally true) and replaced with the true contract (⌘I toggles in place, every lens); fixed Step 4's People-excluded-from-switcher claim (now false — People is a switcher button); retitled ⌘1/⌘2/⌘3 to their lens meanings | adopts ux-simplification-chrome; supersedes prior status: this card was stale on two independent axes even before this push (the `:59-60` inspector claim, per the task-13 brief's own flag) — needs a fresh VM run regardless of the wording fix, per the existing script drift |
| app-006-session-restore | app-006-session-restore.md — quit and relaunch lands Jesse back on the same source + lens (`SessionRestoreState` v2) | Reconciled — NOT re-run | VM e2e | Functional | earlier PASS, but Marcus (persona-7): pre-crash import-completion panel resurrected as a zombie sheet on relaunch; session-scoped panels fixed 6a2364d1 (22861c84); Dana's post-fix relaunch (22861c84) restored picks/People/filter cleanly with no zombie panel, though that leg wasn't explicitly asserted; reconciled 2026-08-09 (Task 13, unified-shell push): largest rewrite in the batch — `SessionRestoreState` v2 persists lens+source (no route), a v1 blob is discarded (no per-field migration), a mid-cull quit relaunches in Grid on the same source (`isRestorableLens`, unconditional `lens != .cull`); replaced the deleted `shouldShowImportCompletionSummary` with `ImportCompletionToastPresentation.toast(for:...)` | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: the PASS covered v1's `selectedView`-route persistence and its legacy-rawValue migration, both gone under v2's hard-cutover no-back-compat model — needs a fresh VM run |
| app-007-go-history | app-007-go-history.md — Go ▸ Back/Forward walk the view-history stacks | Reconciled — NOT re-run | VM e2e | — | PASS; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): the card cited a deleted `SidebarRowTarget` type — history is `[LibrarySource]` (`navigationBackStack`/`navigationForwardStack`); also fixed a stale `Places`-as-sidebar-row example (Places is now the Map lens, not a source) | supersedes prior status: the PASS predates the `LibrarySource`-based history rewrite — needs a fresh VM run |
| app-008-batch-metadata | app-008-batch-metadata.md — ⌥⌘M opens the batch-metadata sheet and its scope picker governs the write | Reconciled — NOT re-run | VM e2e | UX | earlier PASS, but Priya (persona-6): sheet opens prefilled with default Creator/Copyright styled like placeholders, and Apply silently overwrote real provenance on every in-scope photo (catalog + sidecars); blank-means-skip exists but the prefill defeats it; placeholder semantics fixed in aed1dbbe; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘2 preamble ("Library"→"Grid lens") is preamble-only | final-verify run PASS (run-final-verify.md, main@720fb5f5); supersedes prior status: no substantive change, noted for the record per house style |
| app-009-export | app-009-export.md — Export copies with presets and an optional EXIF/IPTC carry | Verified | VM e2e | Functional | Task-12 re-run and PENDING-VM collision batch both PASSed live (one prompt, Replace All/Keep Both/Cancel, no writes before choice, Keep Both suffixes, Replace All overwrites in place) — but Priya (persona-6): "Include EXIF/IPTC metadata" exports carried NONE of the catalog metadata (no keywords/caption/creator/copyright; mdls all null); metadata embedding fixed in aed1dbbe | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| app-010-move-rejects | app-010-move-rejects.md — Move rejects to a folder, then move them back | Verified | VM e2e | UX | Task-12 re-run and Marcus's live folder-move both PASSed (5 jpg+5 xmp moved, paths repointed, Move back flawless) — but the shared confirm-gate/Move-back surface changed in 22861c84 (disabled-until-checked button, truthful Move back) without a fresh drive | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| app-011-find-best-shots | app-011-find-best-shots.md — Find Best Shots routes to Potential Picks, Picks, or a plain-language status | Reconciled — NOT re-run | VM e2e | — | reviewed 2026-08-06 for ghost-derivation staleness — no rename applied; card's "proposals" language refers to the unrelated `.likelyPick` quality-ranking queue, not `AutopilotProposal`/ghosts (source-verified, see card's own Run status); PASS core; 2026-08-09 (Task 13, unified-shell push) confirmed the ⌘2 preamble ("Library"→"Grid lens") is preamble-only | outcome C (nothing-ranked) needs a zero-rank fixture; mark NOT-RUN if unproducible; flagging the brief's proposed "proposals"→"ghosts" rename as incorrect for this card, not applied; supersedes prior status: no substantive change from the preamble fix, noted for the record per house style |
| app-012-autopilot-evaluate-commands | app-012-autopilot-evaluate-commands.md — Run Autopilot and the Evaluate commands gate and report correctly | Reconciled — NOT re-run | VM e2e | — | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`); Steps 4/6 queries became `metadata_json` ghost queries; reconciled 2026-08-09 (Task 13, unified-shell push): `WorkspaceChromePolicy.showsFooter`→`LensChromePolicy.showsFooter`, now gating four browse lenses (Grid/Loupe/Timeline/Map) instead of one workspace | supersedes prior status: the PASS predates the table drop — not valid evidence for the rewritten queries; needs a fresh VM run |
| app-013-diagnostics | app-013-diagnostics.md — Support ▸ Copy Diagnostics puts a truthful report on the pasteboard | Verified | VM e2e | — | PASS; Priya (persona-6) used Copy Diagnostics twice mid-session — output matched app-state ground truth each time (including truthfully reporting the app's own wrong beliefs, e.g. "XMP pending/conflicts: 0/0") | |
| app-014-updater | app-014-updater.md — the Sparkle updater wiring is present and correctly configured (STATIC-ONLY) | Tested-Pass | static + AX | — | static-cap holds | Sparkle e2e untestable pre-release; static-only cap — never promotes. Defaults key names unverified until first live run |
| app-015-preferences | app-015-preferences.md — ⌘, opens Settings and the default byline persists across relaunch | Tested-Pass | VM e2e | — | PASS; Dana opened ⌘, and saw the single byline pane | persona-8 oddity: fresh install showed prefilled "Scenario Tester / © 2026 Scenario" — likely seed artifact, confirm it's not a shipped default |
| app-017-move-rejects-to-trash | app-017-move-rejects-to-trash.md — Move rejects to the macOS Trash, then move them back | Verified | VM e2e (ax+sql+checksums) | UX | trash accounting itself is airtight live (Marcus: 40 jpg+19 xmp → Trash, byte-identical SHA-256 Move back) — but Marcus hit the ghost confirm (full-blue enabled-looking button silently gated by an unchecked box) and the Move-back lie after the user empties Trash (button stays live, silent no-op, no "files are gone" report); fixed in 22861c84 (disabled+hint gate 22861c84, truthful emptied-Trash report f374f33f) | final-verify run PASS (run-final-verify.md, main@720fb5f5) |
| app-018-default-card-destination | app-018-default-card-destination.md — ⌘, sets a default card-import destination that persists and pre-fills new card imports | Verified | VM e2e (ax+sql) + unit | — | 5/5 live PASS on feat branch: section+footer, persist across relaunch, typed-path field pre-filled, zero catalog/metadata writes | panel-skip/Change… covered by unit+presentation tests (not AX-drivable); VM defaults store: use explicit-path form (card Sharp edges) |
| app-016-menu-coverage-invariants | app-016-menu-coverage-invariants.md — every action enum (incl. `lensActionIDs`/`cullSubModeMenuModes`) is mirrored by a menu item (unit-test method) | Reconciled — NOT re-run | unit tests | — | 8/0 cited; reconciled 2026-08-09 (Task 13, unified-shell push): re-anchored on `AppMenuCoveragePresentation.lensActionIDs`/`.cullSubModeMenuModes` and the current 11 tests in `MenuCoveragePresentationTests.swift` (was 8 — 3 tests named that the old roster didn't cover); states the F14 same-commit rule explicitly | unit-test method; gap: Run Autopilot/Scan for Faces/Evaluate Photo/Scope not enumerated — renames uncatchable; DRY-RUN PASSED (8 tests) during authoring; all coverage invariants pass; supersedes prior status: the 8-test count and the action-enum names it cited are both stale — the current tree has 11 tests and no `WorkspaceCommands`/five-tag action set — needs a fresh unit-test run to reconfirm the count |
| dev-001-build-and-run-modes | dev-001-build-and-run-modes.md — build_and_run.sh flag surface | Tested-Pass | host CLI e2e | — | all assertions pass incl. exit codes | usage drift: --real-corpus undocumented; CONFIRMED Documentation defect: usage() omits --real-corpus (live-verified) |
| dev-002-seed-variants | dev-002-seed-variants.md — build_and_run.sh isolated seed variants | Tested-Pass | host CLI e2e | — | pass; 1 flake retry (catalog-init race) |  |
| dev-003-vm-harness | dev-003-vm-harness.md — Tart VM scenario-running harness | Verified | — | Environment | all verbs live incl. key/shell/launch-empty guard | CONFIRMED Documentation defect: --reseed hint names a flag with no dispatch path; sync smoke --reseed would error |
| dev-004-package-release-dry-run | dev-004-package-release-dry-run.md — package_release.sh --dry-run produces an ad-hoc, verified, non-distributable artifact | Tested-Pass | host CLI e2e | — | pass; 1 flake retry (build race) |  |
| dev-005-package-release-signing | dev-005-package-release-signing.md — package_release.sh --sign-only (Developer ID sign, no notarization) | Tested-Pass | static + partial CLI | — | signing path documented, not exercised (cap noted) | needs Developer ID cert locally |
| dev-006-ax-drive | dev-006-ax-drive.md — ax_drive.sh verb surface + host-without-TCC failure mode | Verified | — | Environment | all verbs incl. right-click/modifiers; ⌘-click flaky noted, shift reliable |  |
| dev-007-reset-isolated | dev-007-reset-isolated.md — Isolated test-data reset is marker-gated and running-app-safe | Verified | host CLI e2e | Functional | post-fix re-run all 5 assertions pass (fix 6b25be77 confirmed live) |  |
| dev-008-sample-downloads | dev-008-sample-downloads.md — Sample photo and face-model download scripts | Tested-Fail | host CLI e2e | Logistical | face-model manifest URL is literal REPLACE-ME.example.com — download permanently broken | network |
| dev-009-bench-seeds | dev-009-bench-seeds.md — TeststripBench catalog and fixture seed subcommands | Reconciled — NOT re-run | host CLI e2e | — | reconciled 2026-08-06: removed `autopilot_proposals` from Step 3's captured `.tables` inventory (SP-D0 dropped the table); note: negative-overwrite exits via fatalError/SIGABRT not clean error | supersedes prior status: the pass captured a `.tables` list that included `autopilot_proposals` — not valid evidence for the current schema; needs a fresh host CLI run |
| dev-010-bench-benchmarks | dev-010-bench-benchmarks.md — TeststripBench fast subcommands and summary contract | Tested-Pass | host CLI e2e | — | benchmark-summary contract holds |  |
| dev-011-release-ci | dev-011-release-ci.md — Release workflow vs RELEASING.md static consistency | Tested-Pass | static checks only | — | static-only cap — never promotes past Tested-Pass; 3 secrets still missing | needs tag + 7 secrets; static-only cap. 3 secrets currently missing (cert b64/password, app-specific pw) — next v* tag will fail |
| dev-012-verifier-gates | dev-012-verifier-gates.md — Headless verifier gate, run locally; app-workflow gate, documented only | Tested-Pass | host CLI e2e + solo swift test | — | legs 2-13 live pass; leg1 dispute resolved by controller solo run 1693/0 | Jesse decision: delete-or-keep stale gui verifiers? KNOWN-STALE: verify_evaluation, verify_card_import_path; solo swift test 1693/0 — earlier failure was agent contention (Environment), not product; follow-up: verify_people_clustering.sh presses Evaluate Scope as AXButton (now a menu item), masked by || true |
| cull-029-autopilot-ghost-derivation | cull-029-autopilot-ghost-derivation.md — the unconfirmed AI flag is the only record of a machine proposal; badges/review/sidebar all derive from it and a user override never resurrects | Reconciled — NOT re-run | VM e2e (ax+sql) | — | LIVE RUN 2026-08-06 (Tart VM teststrip-e2e, run dir smoke-1786062713): PASS for every leg the fixture permits. Pre-state and Step 1 PASS; Step 2's own ghost-generation leg NOT-RUN against the documented structural fixture gap (`smoke`'s 900s capture spacing vs. `AssetStackBuilder`'s 2s threshold — 20 ambient keyword ghosts, 0 flag ghosts, exactly as predicted), but its bonus assertion PASSED (sidebar's "Autopilot Proposals" source stays absent with keyword ghosts present and no flag ghosts); Steps 3-7 PASS, driven against two hand-seeded `metadata_json` ghosts (`smoke-1` pick, `smoke-2` reject), not anything autopilot generated live on this fixture — badges, sidebar count, relaunch survival, the primary removal/no-resurrection P0 leg, and completion (no ✨ ceremony) all confirmed; reconciled 2026-08-09 (Task 13 review follow-up, unified-shell push): fixed a `CullSidebarView` orphan citation, the dead `cullSourcePresentation`/`CullSource` concept, the dead "Autopilot Proposals" AX label throughout (→ "AI Suggestions"), Step 4's now-wrong concatenated-AX-string assumption (the current `SidebarRowView` sets separate label/value), leftover "Library" workspace naming in Steps 1/3, and several drifted line citations | resolved both open Sharp edges: the `shell`+`open -n` relaunch mechanism works (no new run dir, `sql smoke` keeps targeting the same catalog), and the sidebar row's live AX text was `"Autopilot Proposals, N"` — now corrected to a separate "AI Suggestions" label + count value, per the card's own 2026-08-09 note; the secondary override-then-clear leg on `smoke-2` isn't separately evidenced by this run and needs a dedicated pass; a stack-bearing fixture (e.g. `burst`) is still needed to exercise live flag-ghost generation (Step 2's own leg); supersedes prior status: the LIVE RUN's Step 4 evidence describes an AX string format the current sidebar row no longer produces — not valid evidence for that step; needs a fresh VM run |
| cull-024-honest-states | cull-024-honest-states.md — the reads panel says "No read yet" before evaluation, and a genuine tie suppresses ✦ with a "too close to call" banner — never a fabricated read | Reconciled — NOT re-run | VM e2e (ax+sql) | — | LIVE RUN 2026-07-28 (app 878f1939, burst fixture): PASS-WITH-CARD-FIXES, no app bugs, Steps 1-5 exercised live, Step 6 a structural no-op on this fixture; 2026-08-06: rewrote the `AutopilotProposalKind` citation-shift note to say the partition was deleted outright by SP-D0, not merely re-shifted (documentation only, no step/assertion changed) | added to LEDGER 2026-08-06 (was previously untracked); supersedes prior status: the LIVE RUN predates the citation-shift note's correction — its own step evidence is unaffected, but the note's line-number claims need independent re-verification |
| cull-025-run-strip-completion | cull-025-run-strip-completion.md — the run strip's stops and windowing agree with the catalog; the completion summary's five counts and gated ceremony actions match the traversal | Reconciled — NOT re-run | VM e2e (ax+sql) | — | reconciled 2026-08-06 to ghost derivation (SP-D0 dropped `autopilot_proposals`): title/intro/Source/Steps/Expected rewritten from six counts (incl. deleted `sparkleAwaiting`) to five, action list from six titles (incl. deleted "Review AI Suggestions") to five, Leg A's seed technique from an `autopilot_proposals` INSERT to a pure `metadata_json` UPDATE; prior LIVE RUN (2026-07-28, app 878f1939) was PASS-WITH-CARD-FIXES for the pre-drop build | added to LEDGER 2026-08-06 (was previously untracked); supersedes prior status: the 2026-07-28 LIVE RUN and its three "Reconciled" sub-notes predate the `autopilot_proposals` drop and the sparkleAwaiting/ceremony deletion — not valid evidence for this revision; needs a fresh VM run |
| cull-026-tentative-never-commits | cull-026-tentative-never-commits.md — a tentative ✨ AI reject counts as undecided everywhere, is never relocated/trashed, and an explicit gesture — confirming it or overriding it — is what actually commits it | Reconciled — NOT re-run | VM e2e (ax+sql) | — | PASS — run live 2026-07-28 (app 878f1939); all 8 steps passed, none touched `autopilot_proposals` so the live evidence is unaffected by SP-D0; 2026-08-06: corrected a stale Source claim that flag-removal had no wired UI ( `U` on a tentative ghost now calls `removeAIField`) and pointed its gap note at cull-029's Step 6 P0 leg | added to LEDGER 2026-08-06 (was previously untracked); the 2026-07-28 PASS stands — only documentation prose was corrected, not any driven assertion |
| people-021-face-group-review | people-021-face-group-review.md — review a face group large, prune it, then name | Reconciled — NOT re-run | VM e2e | — | NOT-RUN — authored alongside the implementation; VM-bound AX driving not executed; reconciled 2026-08-09 (Task 13, unified-shell survivor sweep): Step 2's preamble ("People is no longer ⌘3 — AX-press the Library sub-view toggle") described an intermediate pre-unified-shell era; fixed to ⌘6/the lens switcher's People segment | added to LEDGER 2026-08-09 (was previously untracked); supersedes prior status: no prior run evidence exists to invalidate (still NOT-RUN) |
| people-020-ai-label-provenance | people-020-ai-label-provenance.md — AI labels auto-apply as `origin=ai` (✨), confirm writes the sidecar, and a tentative autopilot reject stays unrelocated until confirmed | Reconciled — NOT re-run | VM e2e (ax+sql) | — | NOT RUN — authored 2026-07-14, source-cited, pending live execution; 2026-08-06: Steps 13-14's `run_id`/`JOIN` against `autopilot_proposals` became a direct `metadata_json` ghost query (SP-D0 dropped the table); Step 18's Commit gesture unaffected | added to LEDGER 2026-08-06 (was previously untracked); supersedes prior status: no prior PASS exists to invalidate — needs a fresh VM run |
| app-019-lens-shell | app-019-lens-shell.md — the unified toolbar lens switcher (six lenses, one sidebar, source/lens orthogonality) | Tested-Pass (11/12 steps) | VM e2e (ax+sql) | — | **live VM run 2026-08-09 @ `8f598239` (smoke): 11 of 12 steps PASS**, no product defects. Steps 1-5, 7-12 all passed: six-button `AXGroup desc="Lens"` in order; deleted Workspace/Library-View controls unreachable; ⌘1-⌘6 subtitles Loupe/Grid/Loupe/Timeline/Map/People with the scope line unchanged across all six and again with `5 Stars` selected; `Analysis Failures` falls back to Grid with `Cull … help="Nothing here is cullable" enabled=false`; `Cull these` matched exact-case while `Cull These` did not match anywhere (driver matching is genuinely case-sensitive) and produced a `culling` session titled `Rating >= 5`; restore returns Timeline after a Timeline quit and Grid after a Cull quit; a 2-photo set scoped Map/Timeline/People to 2 against a 24-photo catalog; Recent-Work reopen kept the Grid lens; the Sets "+" hint fires and the Smart Collections "+" does not | Executable Step 12 reconciled 2026-08-10 from that live evidence: explicitly creates the zero-result/no-selection state and drives exact `AXHeading` label + U+2026-help routes for `New from search…` and `New Set from Selection…`. Status remains **11/12**: Step 6's Stacks *positive* half still lacks the `burst` fixture, and visible tooltip rendering remains method-inconclusive (disabled state + exact AXHelp are proven). This was documentation reconciliation, not a fresh run. |
| import-011-completion-toast-and-import-rows | import-011-completion-toast-and-import-rows.md — the import completion toast, its bell receipt, and the sidebar's import rows | Verified (all 6 targeted re-drive items) | VM e2e (ax+sql) | — | **Step 9 fix verified live 2026-08-09 @ `d70a0f3e`** after RED `78688fa0` and Approved review: fresh empty CARD1 run started in Cull, opened the actual skipped-files child sheet with both filenames, then after Done vended scope `⚠ Skipped files, 4 photos · Import: import-DAAF0507-8797-439D-95D4-17FBCB78B56A`, window `Teststrip – Grid`, and disabled Cull (`enabled=false`, exact help `Nothing here is cullable`). Nested catalog proves scope is the four-photo output set; text files remain two `issues_json` records and zero asset rows. | The preceding `47718d3c` re-drive had already passed the other five targeted items. Executable assertions reconciled 2026-08-10 from recorded live evidence: receipt `Start culling` is `AXLink`; import-row menu is exactly the star toggle plus three import verbs; Step 12 joins snapshot member objects through `json_extract(m.value,'$.rawValue')` instead of the prior vacuous direct-object join. **All six targeted re-drive items remain live-verified**, but this does not claim the historical 12-step card was fully rerun or alter its scoped verdict. |
